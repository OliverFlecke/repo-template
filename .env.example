# Copy to .env (gitignored) for docker-compose to pick up via variable
# substitution. Client id/secret of the Auth0 Machine-to-Machine application
# authorized for the Management API (see libs/Auth0/Auth0Config.cs).
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

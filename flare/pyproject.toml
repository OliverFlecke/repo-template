[project]
	authors         = [{ name = "Oliver Fleckenstein", email = "oliver@flecke.xyz" }]
	dependencies    = ["nvflare>=2.8.1"]
	description     = "Add your description here"
	name            = "flare"
	readme          = "README.md"
	requires-python = ">=3.14"
	version         = "0.1.0"

	[project.scripts]
		flare = "flare:main"

[build-system]
	build-backend = "uv_build"
	requires      = ["uv_build>=0.12.4,<0.13.0"]
